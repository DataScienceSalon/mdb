# Part 6: Prediction
```{r prediction}
# Obtain complete cases with box office information
revenue <- prediction(mod = modelOne$mod, mName = "Model One", case = case, unit = "dollars per day")
votes <- prediction(mod = modelA$mod, mName = "Model Alpha", case = case, unit = "IMDB votes")
case$imdb_num_votes_log <- votes$yHat
revenue2 <- prediction(mod =modelOne$mod, mName = "Model One", case = case, unit = "dollars per day")

```

`r votes$comment`

* * *
